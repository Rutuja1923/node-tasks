const {power, square, squareroot} = require('../modules/exponent');

console.log(`power(2,5) : ${power(2,5)}`);

console.log(`square(10) : ${square(10)}`);

console.log(`squareroot(169) : ${squareroot(169)}`);